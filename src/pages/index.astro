---
import logoHeader from "../../assets/zest-logo-header.png";
import cantoZen from "../../assets/canto-zen.png";
---

<!doctype html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Zest Wellness Station | Parceria para Academias</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&family=DM+Sans:ital,opsz,wght@0,9..40,200..900;1,9..40,200..900&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --cream: #f6f1e7;
        --cream-dark: #ede7d7;
        --green: #7a9a6e;
        --green-dark: #4d7140;
        --green-deep: #2d4a26;
        --peach: #d4845a;
        --peach-light: #e8a882;
        --text: #2c2c2c;
        --text-muted: #7a7068;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      html {
        scroll-behavior: smooth;
      }

      body {
        font-family: "DM Sans", sans-serif;
        background-color: var(--cream);
        color: var(--text);
        overflow-x: hidden;
      }

      .serif {
        font-family: "Cormorant Garamond", serif;
      }

      /* ── Noise texture overlay ── */
      body::before {
        content: "";
        position: fixed;
        inset: 0;
        background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 512 512' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.04'/%3E%3C/svg%3E");
        pointer-events: none;
        z-index: 9999;
        opacity: 0.4;
      }

      /* ── Navigation ── */
      nav {
        position: fixed;
        width: 100%;
        z-index: 1000;
        padding: 1.5rem 2rem;
        transition: all 0.5s cubic-bezier(0.23, 1, 0.32, 1);
      }
      nav.scrolled {
        padding: 0.75rem 2rem;
        background: rgba(246, 241, 231, 0.92);
        backdrop-filter: blur(20px);
        border-bottom: 1px solid rgba(122, 154, 110, 0.15);
        box-shadow: 0 8px 40px rgba(45, 74, 38, 0.06);
      }
      .nav-inner {
        max-width: 1400px;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      .nav-logo {
        display: flex;
        align-items: center;
        gap: 10px;
        text-decoration: none;
      }
      .nav-logo-wordmark {
        font-family: "Cormorant Garamond", serif;
        font-size: 1.2rem;
        font-weight: 700;
        letter-spacing: 0.05em;
        color: var(--green-deep);
      }
      .nav-logo-wordmark em {
        font-style: italic;
        font-weight: 400;
        color: var(--peach);
      }
      .nav-logo-icon {
        width: 38px;
        height: 38px;
        background: var(--green-deep);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .nav-links {
        display: flex;
        gap: 2.5rem;
        align-items: center;
        list-style: none;
      }
      .nav-links a {
        font-size: 0.7rem;
        font-weight: 700;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: var(--green-deep);
        text-decoration: none;
        transition: opacity 0.2s;
      }
      .nav-links a:hover {
        opacity: 0.5;
      }
      .btn-nav {
        background: var(--peach);
        color: white !important;
        padding: 0.6rem 1.5rem;
        border-radius: 100px;
        font-size: 0.65rem !important;
        letter-spacing: 0.2em !important;
        transition: all 0.3s ease !important;
        box-shadow: 0 4px 20px rgba(212, 132, 90, 0.3);
      }
      .btn-nav:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 30px rgba(212, 132, 90, 0.4) !important;
        opacity: 1 !important;
      }

      /* ── Reveal animations ── */
      .reveal {
        opacity: 0;
        transform: translateY(40px);
        transition:
          opacity 0.9s cubic-bezier(0.23, 1, 0.32, 1),
          transform 0.9s cubic-bezier(0.23, 1, 0.32, 1);
      }
      .reveal.visible {
        opacity: 1;
        transform: translateY(0);
      }
      .reveal-delay-1 {
        transition-delay: 0.1s;
      }
      .reveal-delay-2 {
        transition-delay: 0.2s;
      }
      .reveal-delay-3 {
        transition-delay: 0.3s;
      }
      .reveal-delay-4 {
        transition-delay: 0.4s;
      }
      .reveal-delay-5 {
        transition-delay: 0.5s;
      }

      /* ── Hero ── */
      .hero {
        min-height: 100vh;
        display: grid;
        grid-template-columns: 1fr 1fr;
        align-items: center;
        gap: 4rem;
        padding: 8rem 2rem 4rem;
        max-width: 1400px;
        margin: 0 auto;
      }
      @media (max-width: 1024px) {
        .hero {
          grid-template-columns: 1fr;
          padding: 7rem 1.5rem 4rem;
        }
        .hero-visual {
          display: none;
        }
      }
      .hero-eyebrow {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.35rem 1rem;
        background: rgba(122, 154, 110, 0.12);
        border: 1px solid rgba(122, 154, 110, 0.25);
        border-radius: 100px;
        font-size: 0.65rem;
        font-weight: 700;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: var(--green-dark);
        margin-bottom: 1.5rem;
      }
      .hero-title {
        font-family: "Cormorant Garamond", serif;
        font-size: clamp(2.3rem, 3.4vw, 3.9rem);
        font-weight: 600;
        line-height: 1;
        letter-spacing: -0.02em;
        color: var(--green-deep);
        margin-bottom: 1.5rem;
      }
      .hero-title em {
        font-style: italic;
        color: var(--peach);
      }
      .hero-sub {
        font-size: 1.1rem;
        color: var(--text-muted);
        line-height: 1.75;
        max-width: 500px;
        margin-bottom: 2.5rem;
      }
      .hero-ctas {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
      }
      .btn-primary {
        background: var(--green-deep);
        color: white;
        padding: 1rem 2.5rem;
        border-radius: 100px;
        font-size: 0.75rem;
        font-weight: 700;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        text-decoration: none;
        transition: all 0.35s cubic-bezier(0.23, 1, 0.32, 1);
        box-shadow: 0 6px 30px rgba(45, 74, 38, 0.25);
      }
      .btn-primary:hover {
        transform: translateY(-3px);
        box-shadow: 0 12px 40px rgba(45, 74, 38, 0.35);
      }
      .btn-ghost {
        color: var(--green-deep);
        padding: 1rem 2rem;
        border-radius: 100px;
        font-size: 0.75rem;
        font-weight: 700;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        text-decoration: none;
        border: 1.5px solid rgba(45, 74, 38, 0.25);
        transition: all 0.3s ease;
      }
      .btn-ghost:hover {
        border-color: var(--green-deep);
        background: rgba(45, 74, 38, 0.04);
      }

      /* ── Hero stats bar ── */
      .hero-stats {
        display: flex;
        gap: 2rem;
        margin-top: 3rem;
        padding-top: 2.5rem;
        border-top: 1px solid rgba(45, 74, 38, 0.12);
      }
      .hero-stat-num {
        font-family: "Cormorant Garamond", serif;
        font-size: 2.5rem;
        font-weight: 700;
        color: var(--green-deep);
        line-height: 1;
      }
      .hero-stat-label {
        font-size: 0.65rem;
        font-weight: 700;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        color: var(--text-muted);
        margin-top: 4px;
      }

      /* ── Hero visual ── */
      .hero-visual {
        position: relative;
        display: flex;
        justify-content: flex-end;
      }
      .hero-image-wrap {
        position: relative;
        width: 90%;
      }
      .hero-img {
        width: 100%;
        aspect-ratio: 4/5;
        border-radius: 2.5rem;
        background: var(--cream-dark);
        background-image: url("/assets/mercado.png");
        background-size: cover;
        background-position: 12% center;
        box-shadow: 0 60px 120px -20px rgba(45, 74, 38, 0.2);
        overflow: hidden;
        position: relative;
      }
      .hero-img::after {
        content: "";
        position: absolute;
        inset: 0;
        background: linear-gradient(
          to top,
          rgba(45, 74, 38, 0.5) 0%,
          transparent 60%
        );
        border-radius: inherit;
      }
      .hero-badge {
        position: absolute;
        bottom: 2rem;
        left: 2rem;
        right: 2rem;
        background: rgba(246, 241, 231, 0.95);
        backdrop-filter: blur(20px);
        border-radius: 1.5rem;
        padding: 1.25rem 1.5rem;
        display: flex;
        align-items: center;
        gap: 1rem;
        z-index: 2;
        box-shadow: 0 8px 40px rgba(0, 0, 0, 0.15);
      }
      .hero-badge-icon {
        width: 44px;
        height: 44px;
        background: var(--green);
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
      }
      .hero-badge-title {
        font-size: 0.8rem;
        font-weight: 700;
        color: var(--green-deep);
      }
      .hero-badge-sub {
        font-size: 0.65rem;
        color: var(--text-muted);
        margin-top: 2px;
      }
      .hero-tag {
        position: absolute;
        top: 2rem;
        right: -1.5rem;
        background: var(--peach);
        color: white;
        padding: 0.8rem 1.2rem;
        border-radius: 1.25rem;
        font-size: 0.65rem;
        font-weight: 700;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        box-shadow: 0 8px 30px rgba(212, 132, 90, 0.35);
        writing-mode: horizontal-tb;
      }
      /* Floating dots */
      .float-dot {
        position: absolute;
        border-radius: 50%;
        animation: float-anim 6s ease-in-out infinite;
      }
      @keyframes float-anim {
        0%,
        100% {
          transform: translateY(0);
        }
        50% {
          transform: translateY(-15px);
        }
      }

      /* ── Section shared ── */
      .section-label {
        font-size: 0.65rem;
        font-weight: 700;
        letter-spacing: 0.25em;
        text-transform: uppercase;
        color: var(--green);
        margin-bottom: 1rem;
      }
      .section-title {
        font-family: "Cormorant Garamond", serif;
        font-size: clamp(2.2rem, 4vw, 3.5rem);
        font-weight: 600;
        line-height: 1.1;
        color: var(--green-deep);
      }
      .section-title em {
        font-style: italic;
        color: var(--peach);
      }

      /* ── Problem section ── */
      .problem-section {
        padding: 8rem 2rem;
        max-width: 1400px;
        margin: 0 auto;
      }
      .problem-header {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 4rem;
        align-items: end;
        margin-bottom: 4rem;
      }
      @media (max-width: 768px) {
        .problem-header {
          grid-template-columns: 1fr;
        }
      }
      .problem-quote {
        font-family: "Cormorant Garamond", serif;
        font-size: 1.4rem;
        font-style: italic;
        color: var(--text-muted);
        line-height: 1.6;
        padding-left: 1.5rem;
        border-left: 3px solid var(--peach);
      }
      .problem-cards {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 1.5rem;
      }
      @media (max-width: 900px) {
        .problem-cards {
          grid-template-columns: 1fr;
        }
      }
      .problem-card {
        background: white;
        border-radius: 2rem;
        padding: 2.5rem;
        border: 1px solid rgba(122, 154, 110, 0.12);
        transition: all 0.4s cubic-bezier(0.23, 1, 0.32, 1);
        cursor: default;
        position: relative;
        overflow: hidden;
      }
      .problem-card::before {
        content: "";
        position: absolute;
        inset: 0;
        background: linear-gradient(
          135deg,
          rgba(122, 154, 110, 0.04),
          transparent
        );
        opacity: 0;
        transition: opacity 0.4s;
      }
      .problem-card:hover {
        transform: translateY(-6px);
        box-shadow: 0 30px 60px rgba(45, 74, 38, 0.1);
      }
      .problem-card:hover::before {
        opacity: 1;
      }
      .problem-card-number {
        font-family: "Cormorant Garamond", serif;
        font-size: 5rem;
        font-weight: 300;
        color: rgba(122, 154, 110, 0.15);
        line-height: 1;
        margin-bottom: 1rem;
      }
      .problem-card-icon {
        width: 48px;
        height: 48px;
        border-radius: 14px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1.5rem;
      }
      .problem-card h3 {
        font-family: "Cormorant Garamond", serif;
        font-size: 1.5rem;
        font-weight: 600;
        color: var(--green-deep);
        margin-bottom: 0.75rem;
      }
      .problem-card p {
        font-size: 0.9rem;
        color: var(--text-muted);
        line-height: 1.75;
      }

      /* ── Metrics band ── */
      .metrics-band {
        background: var(--green-deep);
        padding: 4rem 2rem;
        position: relative;
        overflow: hidden;
      }
      .metrics-band::before {
        content: "";
        position: absolute;
        top: -50%;
        right: -10%;
        width: 600px;
        height: 600px;
        background: radial-gradient(
          circle,
          rgba(122, 154, 110, 0.15),
          transparent 70%
        );
        pointer-events: none;
      }
      .metrics-inner {
        max-width: 1400px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 2rem;
        text-align: center;
      }
      @media (max-width: 768px) {
        .metrics-inner {
          grid-template-columns: repeat(2, 1fr);
        }
      }
      .metric-num {
        font-family: "Cormorant Garamond", serif;
        font-size: 3.5rem;
        font-weight: 700;
        color: white;
        line-height: 1;
        margin-bottom: 0.5rem;
      }
      .metric-label {
        font-size: 0.65rem;
        font-weight: 700;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: rgba(255, 255, 255, 0.5);
      }
      .metric-divider {
        width: 30px;
        height: 2px;
        background: var(--peach);
        margin: 0.75rem auto;
      }

      /* ── Solution section ── */
      .solution-section {
        padding: 8rem 2rem;
        max-width: 1400px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 6rem;
        align-items: center;
      }
      @media (max-width: 1024px) {
        .solution-section {
          grid-template-columns: 1fr;
        }
      }
      .solution-steps {
        display: flex;
        flex-direction: column;
        gap: 0;
      }
      .solution-step {
        display: flex;
        gap: 2rem;
        padding: 2rem 0;
        border-bottom: 1px solid rgba(45, 74, 38, 0.08);
        transition: all 0.3s ease;
        cursor: default;
      }
      .solution-step:first-child {
        padding-top: 0;
      }
      .solution-step:last-child {
        border-bottom: none;
      }
      .solution-step:hover .step-num {
        background: var(--green-deep);
        color: white;
      }
      .step-num {
        flex-shrink: 0;
        width: 52px;
        height: 52px;
        border: 2px solid rgba(45, 74, 38, 0.15);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: "Cormorant Garamond", serif;
        font-size: 1.1rem;
        font-weight: 700;
        color: var(--green-deep);
        transition: all 0.3s ease;
      }
      .step-title {
        font-size: 0.65rem;
        font-weight: 700;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: var(--green);
        margin-bottom: 0.5rem;
      }
      .step-body {
        font-size: 0.95rem;
        color: var(--text-muted);
        line-height: 1.75;
      }

      .solution-right {
      }
      .solution-pullquote {
        background: white;
        border: 1px solid rgba(122, 154, 110, 0.15);
        border-radius: 2rem;
        padding: 2.5rem;
        margin-top: 2.5rem;
        position: relative;
      }
      .pullquote-mark {
        font-family: "Cormorant Garamond", serif;
        font-size: 6rem;
        color: rgba(212, 132, 90, 0.2);
        line-height: 0.5;
        display: block;
        margin-bottom: 1rem;
      }
      .pullquote-text {
        font-family: "Cormorant Garamond", serif;
        font-size: 1.3rem;
        font-style: italic;
        color: var(--green-deep);
        line-height: 1.6;
      }

      /* ── Personalization section ── */
      .personalization-section {
        padding: 8rem 2rem;
        max-width: 1400px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 5rem;
        align-items: center;
      }
      @media (max-width: 1024px) {
        .personalization-section {
          grid-template-columns: 1fr;
          gap: 2.5rem;
          padding: 6rem 1.5rem;
        }
      }
      .personalization-copy {
        max-width: 560px;
      }
      .personalization-text {
        font-size: 0.95rem;
        color: var(--text-muted);
        line-height: 1.8;
        margin: 1.5rem 0 2rem;
      }
      .personalization-chips {
        display: flex;
        flex-wrap: wrap;
        gap: 0.75rem;
      }
      .personalization-chip {
        display: inline-flex;
        align-items: center;
        padding: 0.45rem 1rem;
        border-radius: 999px;
        font-size: 0.68rem;
        font-weight: 700;
        letter-spacing: 0.12em;
        text-transform: uppercase;
        border: 1px solid rgba(122, 154, 110, 0.25);
        background: rgba(122, 154, 110, 0.1);
        color: var(--green-dark);
      }
      .personalization-media {
        position: relative;
      }
      .personalization-image-card {
        background: white;
        border-radius: 2rem;
        padding: 0.9rem;
        border: 1px solid rgba(122, 154, 110, 0.15);
        box-shadow: 0 30px 60px rgba(45, 74, 38, 0.12);
      }
      .personalization-image {
        width: 100%;
        display: block;
        border-radius: 1.4rem;
        object-fit: cover;
        max-height: 540px;
      }
      .personalization-badge {
        position: absolute;
        bottom: -1rem;
        left: 1.5rem;
        background: rgba(246, 241, 231, 0.95);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(122, 154, 110, 0.18);
        border-radius: 1rem;
        padding: 0.75rem 1rem;
        font-size: 0.65rem;
        font-weight: 800;
        letter-spacing: 0.14em;
        text-transform: uppercase;
        color: var(--green-deep);
      }
      @media (max-width: 768px) {
        .personalization-badge {
          position: static;
          margin-top: 1rem;
          display: inline-flex;
        }
      }

      /* ── Comparison section ── */
      .comparison-section {
        padding: 8rem 2rem;
        background: linear-gradient(135deg, var(--green-deep) 0%, #1a3314 100%);
        position: relative;
        overflow: hidden;
      }
      .comparison-section::before {
        content: "";
        position: absolute;
        bottom: 0;
        left: -10%;
        width: 70%;
        height: 70%;
        background: radial-gradient(
          ellipse,
          rgba(212, 132, 90, 0.08),
          transparent 70%
        );
        pointer-events: none;
      }
      .comparison-inner {
        max-width: 1000px;
        margin: 0 auto;
      }
      .comparison-table {
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 2rem;
        overflow: hidden;
        backdrop-filter: blur(10px);
        margin-top: 3rem;
      }
      .comparison-header {
        display: grid;
        grid-template-columns: 1.4fr 1.8fr 1.8fr;
        padding: 1.5rem 2.5rem;
        background: rgba(255, 255, 255, 0.06);
        border-bottom: 1px solid rgba(255, 255, 255, 0.08);
      }
      .comparison-header-cell {
        font-size: 0.6rem;
        font-weight: 700;
        letter-spacing: 0.25em;
        text-transform: uppercase;
        color: rgba(255, 255, 255, 0.4);
      }
      .comparison-header-cell.highlight {
        color: var(--peach-light);
      }
      .comparison-row {
        display: grid;
        grid-template-columns: 1.4fr 1.8fr 1.8fr;
        padding: 1.5rem 2.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        align-items: center;
        transition: background 0.2s;
      }
      .comparison-row:last-child {
        border-bottom: none;
      }
      .comparison-row:hover {
        background: rgba(255, 255, 255, 0.03);
      }
      .comparison-feature {
        font-size: 0.85rem;
        font-weight: 600;
        color: rgba(255, 255, 255, 0.85);
      }
      .comparison-val {
        font-size: 0.8rem;
        color: rgba(255, 255, 255, 0.4);
      }
      .comparison-val.good {
        color: #a8d89a;
        font-weight: 700;
        display: flex;
        align-items: center;
        gap: 0.4rem;
      }
      .comparison-val.good::before {
        content: "";
        width: 6px;
        height: 6px;
        background: #a8d89a;
        border-radius: 50%;
        flex-shrink: 0;
      }

      /* ── Contact section ── */
      .contact-section {
        padding: 8rem 2rem;
        max-width: 1400px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 6rem;
        align-items: start;
      }
      @media (max-width: 1024px) {
        .contact-section {
          grid-template-columns: 1fr;
          gap: 3rem;
        }
      }
      .contact-left {
      }
      .contact-benefits {
        margin-top: 3rem;
        display: flex;
        flex-direction: column;
        gap: 1.25rem;
      }
      .contact-benefit {
        display: flex;
        align-items: flex-start;
        gap: 1rem;
      }
      .benefit-check {
        width: 24px;
        height: 24px;
        background: rgba(122, 154, 110, 0.15);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
        margin-top: 2px;
      }
      .benefit-text {
        font-size: 0.9rem;
        color: var(--text-muted);
        line-height: 1.6;
      }
      .benefit-text strong {
        color: var(--text);
      }

      .contact-form-wrap {
        background: white;
        border-radius: 2.5rem;
        padding: 3rem;
        border: 1px solid rgba(122, 154, 110, 0.12);
        box-shadow: 0 40px 80px rgba(45, 74, 38, 0.08);
      }
      .form-group {
        margin-bottom: 1.5rem;
      }
      .form-label {
        display: block;
        font-size: 0.6rem;
        font-weight: 800;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: var(--green-dark);
        margin-bottom: 0.6rem;
      }
      .form-input {
        width: 100%;
        padding: 1rem 1.25rem;
        border: 1.5px solid rgba(122, 154, 110, 0.2);
        border-radius: 1rem;
        font-family: "DM Sans", sans-serif;
        font-size: 0.9rem;
        color: var(--text);
        background: var(--cream);
        outline: none;
        transition: all 0.3s ease;
      }
      .form-input:focus {
        border-color: var(--green);
        background: white;
        box-shadow: 0 0 0 4px rgba(122, 154, 110, 0.08);
      }
      .form-input::placeholder {
        color: rgba(122, 112, 104, 0.5);
      }
      .form-row {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1.25rem;
      }
      @media (max-width: 600px) {
        .form-row {
          grid-template-columns: 1fr;
        }
      }
      .btn-submit {
        width: 100%;
        padding: 1.1rem;
        background: var(--green-deep);
        color: white;
        border: none;
        border-radius: 1rem;
        font-family: "DM Sans", sans-serif;
        font-size: 0.75rem;
        font-weight: 700;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        cursor: pointer;
        transition: all 0.35s cubic-bezier(0.23, 1, 0.32, 1);
        box-shadow: 0 6px 30px rgba(45, 74, 38, 0.25);
        margin-top: 0.75rem;
      }
      .btn-submit:hover {
        transform: translateY(-2px);
        box-shadow: 0 12px 40px rgba(45, 74, 38, 0.35);
      }
      .btn-submit:active {
        transform: translateY(0);
      }
      .form-disclaimer {
        text-align: center;
        font-size: 0.65rem;
        font-weight: 700;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        color: rgba(122, 112, 104, 0.5);
        margin-top: 1rem;
      }
      .success-msg {
        display: none;
        text-align: center;
        padding: 3rem 0;
      }
      .success-icon {
        width: 72px;
        height: 72px;
        background: linear-gradient(135deg, var(--green), var(--green-dark));
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1.5rem;
        box-shadow: 0 12px 40px rgba(122, 154, 110, 0.3);
      }

      /* ── Footer ── */
      footer {
        padding: 4rem 2rem;
        border-top: 1px solid rgba(45, 74, 38, 0.08);
        background: rgba(255, 255, 255, 0.4);
      }
      .footer-inner {
        max-width: 1400px;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        gap: 2rem;
      }
      .footer-links {
        display: flex;
        gap: 2rem;
        align-items: center;
      }
      .footer-links a {
        font-size: 0.65rem;
        font-weight: 700;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        color: var(--text-muted);
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        transition: color 0.2s;
      }
      .footer-links a:hover {
        color: var(--peach);
      }
      .footer-copy {
        font-size: 0.6rem;
        font-weight: 700;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: rgba(122, 112, 104, 0.4);
      }

      /* ── Mobile CTA ── */
      .mobile-cta {
        display: none;
        position: fixed;
        bottom: 1.5rem;
        left: 1.5rem;
        right: 1.5rem;
        z-index: 500;
      }
      @media (max-width: 768px) {
        .mobile-cta {
          display: block;
        }
      }
      .mobile-cta a {
        display: block;
        background: var(--green-deep);
        color: white;
        text-align: center;
        padding: 1.1rem;
        border-radius: 1rem;
        font-size: 0.75rem;
        font-weight: 700;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        text-decoration: none;
        box-shadow: 0 10px 40px rgba(45, 74, 38, 0.35);
      }

      /* ── Horizontal marquee strip ── */
      .marquee-section {
        background: var(--cream-dark);
        padding: 1.5rem 0;
        overflow: hidden;
        border-top: 1px solid rgba(45, 74, 38, 0.06);
        border-bottom: 1px solid rgba(45, 74, 38, 0.06);
      }
      .marquee-track {
        display: flex;
        width: max-content;
        animation: marquee 25s linear infinite;
      }
      .marquee-item {
        display: flex;
        align-items: center;
        gap: 1.5rem;
        padding: 0 2.5rem;
        font-size: 0.65rem;
        font-weight: 800;
        letter-spacing: 0.25em;
        text-transform: uppercase;
        color: var(--green-dark);
        white-space: nowrap;
      }
      .marquee-sep {
        width: 4px;
        height: 4px;
        background: var(--peach);
        border-radius: 50%;
      }
      @keyframes marquee {
        0% {
          transform: translateX(0);
        }
        100% {
          transform: translateX(-50%);
        }
      }
      @media (max-width: 768px) {
        nav {
          padding: 0.6rem 0.75rem 0.5rem;
        }
        nav.scrolled {
          padding: 0.5rem 0.75rem 0.45rem;
        }
        .nav-inner {
          flex-direction: column;
          align-items: center;
          gap: 0.75rem;
        }
        .nav-logo {
          justify-content: center;
          gap: 0.4rem;
        }
        .nav-logo-icon {
          width: 34px;
          height: 34px;
        }
        .nav-logo-icon img {
          width: 34px !important;
          height: 34px !important;
        }
        .nav-logo-wordmark {
          font-size: 1rem;
          line-height: 1;
        }
        .nav-links {
          width: auto;
          justify-content: center;
          gap: 0.85rem;
          flex-wrap: wrap;
        }
        .nav-links a {
          font-size: 0.55rem;
          letter-spacing: 0.14em;
        }
        .btn-nav {
          padding: 0.45rem 0.8rem;
          font-size: 0.54rem !important;
          letter-spacing: 0.14em !important;
          white-space: nowrap;
        }

        .hero {
          padding: 7.1rem 1rem 3rem;
          gap: 2rem;
        }
        .hero-title {
          font-size: clamp(2.25rem, 10.5vw, 2.9rem);
          line-height: 0.98;
        }
        .hero-title-break {
          display: none;
        }
        .hero-sub {
          font-size: 0.95rem;
          line-height: 1.6;
        }

        .comparison-section {
          padding: 5rem 0.9rem;
        }
        .comparison-table {
          margin-top: 2rem;
          border-radius: 1.25rem;
        }
        .comparison-header {
          padding: 1rem 1rem;
        }
        .comparison-row {
          padding: 1rem 1rem;
        }
      }
    </style>
  </head>
  <body>
    <!-- Navigation -->
    <nav id="nav">
      <div class="nav-inner">
        <a href="#" class="nav-logo">
          <div
            class="nav-logo-icon"
            style="background: transparent; border: none"
          >
            <img
              src={logoHeader.src}
              alt="Zest Logo"
              style="width: 42px; height: 42px; object-fit: contain"
            />
          </div>
          <span class="nav-logo-wordmark">
            ZEST
            <em>Wellness</em>
          </span>
        </a>
        <ul class="nav-links">
          <li><a href="#problema">Benefícios</a></li>
          <li><a href="#solucao">A Solução</a></li>
          <li><a href="#contato" class="btn-nav">Seja Parceiro</a></li>
        </ul>
      </div>
    </nav>

    <!-- Hero -->
    <section
      style="background: var(--cream); position: relative; overflow: hidden"
    >
      <!-- Subtle bg decoration -->
      <div
        style="
          position: absolute;
          top: -10%;
          right: -5%;
          width: 700px;
          height: 700px;
          background: radial-gradient(
            circle,
            rgba(122, 154, 110, 0.07),
            transparent 70%
          );
          pointer-events: none;
        "
      ></div>
      <div
        style="
          position: absolute;
          bottom: -10%;
          left: -5%;
          width: 500px;
          height: 500px;
          background: radial-gradient(
            circle,
            rgba(212, 132, 90, 0.06),
            transparent 70%
          );
          pointer-events: none;
        "
      ></div>

      <div class="hero">
        <div class="hero-content">
          <div class="hero-eyebrow reveal">
            B2B Partnership · Fitness & Wellness
          </div>
          <h1 class="hero-title reveal reveal-delay-1">
            Conveniência no seu espaço,<br class="hero-title-break" />sem complicar
            a <em>operação.</em>
          </h1>
          <p class="hero-sub reveal reveal-delay-2">
            Automatize as micro-transações do balcão e ofereça conveniência
            premium aos seus alunos com a curadoria inteligente da Zest — sem
            custo fixo para a sua academia.
          </p>
          <div class="hero-ctas reveal reveal-delay-3">
            <a href="#contato" class="btn-primary">Quero ativar na academia</a>
            <a href="#problema" class="btn-ghost">Ver como funciona</a>
          </div>
          <div class="hero-stats reveal reveal-delay-4">
            <div>
              <div class="hero-stat-num" data-count="24">0</div>
              <div class="hero-stat-label">Horas ativo</div>
            </div>
            <div style="width: 1px; background: rgba(45, 74, 38, 0.12)"></div>
            <div>
              <div
                class="hero-stat-num"
                data-count="0"
                data-prefix=""
                data-suffix="%"
              >
                0%
              </div>
              <div class="hero-stat-label">Custo fixo</div>
            </div>
            <div style="width: 1px; background: rgba(45, 74, 38, 0.12)"></div>
            <div>
              <div class="hero-stat-num" data-count="100" data-suffix="%">
                0%
              </div>
              <div class="hero-stat-label">Automatizado</div>
            </div>
          </div>
        </div>
        <div class="hero-visual">
          <div
            class="float-dot"
            style="
              width: 14px;
              height: 14px;
              background: var(--peach);
              opacity: 0.5;
              top: 10%;
              left: -5%;
              animation-delay: 0s;
            "
          ></div>
          <div
            class="float-dot"
            style="
              width: 8px;
              height: 8px;
              background: var(--green);
              opacity: 0.4;
              top: 30%;
              right: 5%;
              animation-delay: 2s;
            "
          ></div>
          <div
            class="float-dot"
            style="
              width: 20px;
              height: 20px;
              background: var(--green-dark);
              opacity: 0.15;
              bottom: 20%;
              left: 0%;
              animation-delay: 4s;
            "
          ></div>
          <div class="hero-image-wrap reveal reveal-delay-2">
            <div class="hero-img"></div>
            <div class="hero-badge">
              <div class="hero-badge-icon">
                <svg
                  width="20"
                  height="20"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="white"
                  stroke-width="2"
                >
                  <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"></polyline>
                </svg>
              </div>
              <div>
                <div class="hero-badge-title">Wellness Station Ativa</div>
                <div class="hero-badge-sub">
                  Autoatendimento 24h · Gestão por dados
                </div>
              </div>
            </div>
            <div class="hero-tag">Instalação rápida</div>
          </div>
        </div>
      </div>
    </section>

    <!-- Marquee strip -->
    <div class="marquee-section">
      <div class="marquee-track">
        <span class="marquee-item"
          >Autoatendimento 24/7 <span class="marquee-sep"></span
        ></span>
        <span class="marquee-item"
          >Zero custo fixo <span class="marquee-sep"></span
        ></span>
        <span class="marquee-item"
          >Gestão inteligente de estoque <span class="marquee-sep"></span
        ></span>
        <span class="marquee-item"
          >Mix baseado em dados <span class="marquee-sep"></span
        ></span>
        <span class="marquee-item"
          >Instalação em 48h <span class="marquee-sep"></span
        ></span>
        <span class="marquee-item"
          >Suporte dedicado <span class="marquee-sep"></span
        ></span>
        <span class="marquee-item"
          >Sem risco de inventário <span class="marquee-sep"></span
        ></span>
        <span class="marquee-item"
          >Foco em retenção de alunos <span class="marquee-sep"></span
        ></span>
        <!-- duplicado para loop contínuo -->
        <span class="marquee-item"
          >Autoatendimento 24/7 <span class="marquee-sep"></span
        ></span>
        <span class="marquee-item"
          >Zero custo fixo <span class="marquee-sep"></span
        ></span>
        <span class="marquee-item"
          >Gestão inteligente de estoque <span class="marquee-sep"></span
        ></span>
        <span class="marquee-item"
          >Mix baseado em dados <span class="marquee-sep"></span
        ></span>
        <span class="marquee-item"
          >Instalação em 48h <span class="marquee-sep"></span
        ></span>
        <span class="marquee-item"
          >Suporte dedicado <span class="marquee-sep"></span
        ></span>
        <span class="marquee-item"
          >Sem risco de inventário <span class="marquee-sep"></span
        ></span>
        <span class="marquee-item"
          >Foco em retenção de alunos <span class="marquee-sep"></span
        ></span>
      </div>
    </div>

    <!-- Problem Section -->
    <section id="problema" class="problem-section">
      <div class="problem-header">
        <div class="reveal">
          <p class="section-label">O problema real</p>
          <h2 class="section-title">
            Sua equipe está resolvendo o problema <em>errado.</em>
          </h2>
        </div>
        <div class="problem-quote reveal reveal-delay-2">
          "Enquanto a recepção passa um isotônico no cartão, um novo aluno
          desiste de esperar pelo tour de vendas."
        </div>
      </div>
      <div class="problem-cards">
        <div class="problem-card reveal">
          <div class="problem-card-number">01</div>
          <div
            class="problem-card-icon"
            style="background: rgba(122, 154, 110, 0.12)"
          >
            <svg
              width="22"
              height="22"
              viewBox="0 0 24 24"
              fill="none"
              stroke="var(--green)"
              stroke-width="2"
            >
              <polyline points="23 6 13.5 15.5 8.5 10.5 1 18"></polyline>
              <polyline points="17 6 23 6 23 12"></polyline>
            </svg>
          </div>
          <h3>Gargalo Comercial</h3>
          <p>
            Cada minuto em micro-transações de balcão é um minuto a menos em
            prospecção, renovações e fechamento de novos planos.
          </p>
        </div>
        <div class="problem-card reveal reveal-delay-2">
          <div class="problem-card-number">02</div>
          <div
            class="problem-card-icon"
            style="background: rgba(122, 154, 110, 0.12)"
          >
            <svg
              width="22"
              height="22"
              viewBox="0 0 24 24"
              fill="none"
              stroke="var(--green)"
              stroke-width="2"
            >
              <circle cx="12" cy="12" r="10"></circle>
              <line x1="12" y1="8" x2="12" y2="12"></line>
              <line x1="12" y1="16" x2="12.01" y2="16"></line>
            </svg>
          </div>
          <h3>Fricção Desnecessária</h3>
          <p>
            Filas para comprar água ou suplementos geram estresse na equipe e
            prejudicam a experiência de quem quer apenas treinar.
          </p>
        </div>
        <div class="problem-card reveal reveal-delay-3">
          <div class="problem-card-number">03</div>
          <div
            class="problem-card-icon"
            style="background: rgba(45, 74, 38, 0.08)"
          >
            <svg
              width="22"
              height="22"
              viewBox="0 0 24 24"
              fill="none"
              stroke="var(--green-deep)"
              stroke-width="2"
            >
              <rect x="2" y="3" width="20" height="14" rx="2" ry="2"></rect>
              <line x1="8" y1="21" x2="16" y2="21"></line>
              <line x1="12" y1="17" x2="12" y2="21"></line>
            </svg>
          </div>
          <h3>Estoque sem Controle</h3>
          <p>
            Perdas por vencimento, ruptura de estoque e reposição manual consomem
            margem e tempo de gestão que poderiam estar em outro lugar.
          </p>
        </div>
      </div>
    </section>

    <!-- Metrics Band -->
    <div class="metrics-band">
      <div class="metrics-inner">
        <div class="reveal">
          <div class="metric-num counter" data-target="24">0</div>
          <div class="metric-divider"></div>
          <div class="metric-label">Horas de operação</div>
        </div>
        <div class="reveal reveal-delay-1">
          <div
            class="metric-num counter"
            data-target="0"
            data-prefix="R$"
            data-suffix=""
          >
            R$0
          </div>
          <div class="metric-divider"></div>
          <div class="metric-label">Custo fixo para a academia</div>
        </div>
        <div class="reveal reveal-delay-2">
          <div class="metric-num counter" data-target="48" data-suffix="h">
            0h
          </div>
          <div class="metric-divider"></div>
          <div class="metric-label">Para instalar e operar</div>
        </div>
        <div class="reveal reveal-delay-3">
          <div class="metric-num counter" data-target="100" data-suffix="%">
            0%
          </div>
          <div class="metric-divider"></div>
          <div class="metric-label">Automatizado</div>
        </div>
      </div>
    </div>

    <!-- Solution Section -->
    <section id="solucao" style="background: var(--cream); padding: 0">
      <div class="solution-section">
        <div class="solution-left reveal">
          <p class="section-label">Como funciona</p>
          <h2 class="section-title" style="margin-bottom: 3rem">
            Mais retenção. Menos<br />trabalho <em>operacional.</em>
          </h2>
          <div class="solution-steps">
            <div class="solution-step">
              <div class="step-num">01</div>
              <div>
                <div class="step-title">Vendas 100% Automáticas</div>
                <p class="step-body">
                  Autoatendimento inteligente disponível 24/7, eliminando erros
                  de estoque e a necessidade de equipe extra para controle.
                </p>
              </div>
            </div>
            <div class="solution-step">
              <div class="step-num">02</div>
              <div>
                <div class="step-title">Recepção Estratégica</div>
                <p class="step-body">
                  Sua equipe deixa de ser operacional e volta a ser consultiva
                  — focada em vendas de matrículas e atendimento de alto nível.
                </p>
              </div>
            </div>
            <div class="solution-step">
              <div class="step-num">03</div>
              <div>
                <div class="step-title">Mix Guiado por Dados</div>
                <p class="step-body">
                  Não são apenas snacks. São os produtos de pré e pós-treino que
                  seu aluno realmente procura, definidos por dados de consumo
                  real.
                </p>
              </div>
            </div>
            <div class="solution-step">
              <div class="step-num">04</div>
              <div>
                <div class="step-title">Retenção Aumentada</div>
                <p class="step-body">
                  Quando o aluno encontra tudo o que precisa na sua academia, a
                  barreira para manter a rotina diminui e a fidelidade cresce.
                </p>
              </div>
            </div>
          </div>
        </div>
        <div class="solution-right reveal reveal-delay-2">
          <p class="section-label">Impacto real</p>
          <h2
            class="section-title"
            style="font-size: clamp(1.8rem, 3vw, 2.8rem); margin-bottom: 1.5rem"
          >
            Sua academia como um hub de <em>bem-estar completo.</em>
          </h2>
          <p
            style="
              font-size: 0.95rem;
              color: var(--text-muted);
              line-height: 1.8;
              margin-bottom: 2rem;
            "
          >
            A expertise da Zest transforma a sua unidade num destino. Hidratação
            premium, nutrição funcional e conveniência inteligente — tudo curado
            para o perfil dos seus alunos.
          </p>
          <div class="solution-pullquote">
            <span class="pullquote-mark">"</span>
            <p class="pullquote-text">
              A conveniência é a nova moeda da fidelidade. Transforme o seu
              espaço físico num ecossistema inteligente de consumo.
            </p>
          </div>
          <!-- Mini feature chips -->
          <div
            style="
              display: flex;
              flex-wrap: wrap;
              gap: 0.75rem;
              margin-top: 2rem;
            "
          >
            <span
              style="
                padding: 0.4rem 1rem;
                background: rgba(122, 154, 110, 0.1);
                border: 1px solid rgba(122, 154, 110, 0.2);
                border-radius: 100px;
                font-size: 0.7rem;
                font-weight: 700;
                letter-spacing: 0.1em;
                color: var(--green-dark);
              "
              >Sem risco de inventário</span
            >
            <span
              style="
                padding: 0.4rem 1rem;
                background: rgba(122, 154, 110, 0.1);
                border: 1px solid rgba(122, 154, 110, 0.2);
                border-radius: 100px;
                font-size: 0.7rem;
                font-weight: 700;
                letter-spacing: 0.1em;
                color: var(--green-dark);
              "
              >Instalação rápida</span
            >
            <span
              style="
                padding: 0.4rem 1rem;
                background: rgba(212, 132, 90, 0.1);
                border: 1px solid rgba(212, 132, 90, 0.2);
                border-radius: 100px;
                font-size: 0.7rem;
                font-weight: 700;
                letter-spacing: 0.1em;
                color: var(--peach);
              "
              >Suporte dedicado</span
            >
            <span
              style="
                padding: 0.4rem 1rem;
                background: rgba(212, 132, 90, 0.1);
                border: 1px solid rgba(212, 132, 90, 0.2);
                border-radius: 100px;
                font-size: 0.7rem;
                font-weight: 700;
                letter-spacing: 0.1em;
                color: var(--peach);
              "
              >Relatórios mensais</span
            >
          </div>
        </div>
      </div>
    </section>

    <!-- Personalization -->
    <section style="background: white">
      <div class="personalization-section">
        <div class="personalization-copy reveal">
          <p class="section-label">Diferencial de marca</p>
          <h2 class="section-title">
            Transformamos áreas livres em <em>espaços de descanso</em> com
            identidade.
          </h2>
          <p class="personalization-text">
            A Zest vai além da estação de conveniência: desenhamos pequenas
            intervenções no ambiente do parceiro para criar pontos de pausa,
            acolhimento e permanência. O resultado é um espaço mais convidativo,
            com percepção premium e maior conexão do aluno com a sua marca.
          </p>
          <div class="personalization-chips">
            <span class="personalization-chip">Ambiente mais acolhedor</span>
            <span class="personalization-chip">Valorização da área ociosa</span>
            <span class="personalization-chip">Experiência premium</span>
          </div>
        </div>
        <div class="personalization-media reveal reveal-delay-2">
          <div class="personalization-image-card">
            <img
              src={cantoZen.src}
              alt="Exemplo de personalização da Zest com espaço de descanso na área livre do parceiro"
              class="personalization-image"
            />
          </div>
          <span class="personalization-badge">Intervenção leve e estratégica</span>
        </div>
      </div>
    </section>

    <!-- Comparison -->
    <section class="comparison-section">
      <div class="comparison-inner">
        <div style="text-align: center" class="reveal">
          <p class="section-label" style="color: rgba(255, 255, 255, 0.4)">
            Comparativo
          </p>
          <h2 class="section-title" style="color: white; font-style: italic">
            O salto de eficiência
          </h2>
        </div>
        <div class="comparison-table reveal reveal-delay-1">
          <div class="comparison-header">
            <div class="comparison-header-cell">Recurso</div>
            <div class="comparison-header-cell">Modelo Tradicional</div>
            <div class="comparison-header-cell highlight">Wellness Station</div>
          </div>
          <div class="comparison-row">
            <div class="comparison-feature">Carga de trabalho</div>
            <div class="comparison-val">Alta — venda manual + inventário</div>
            <div class="comparison-val good">Zero — 100% automatizado</div>
          </div>
          <div class="comparison-row">
            <div class="comparison-feature">Risco de estoque</div>
            <div class="comparison-val">Perdas e vencimentos constantes</div>
            <div class="comparison-val good">Gestão inteligente por dados</div>
          </div>
          <div class="comparison-row">
            <div class="comparison-feature">Disponibilidade</div>
            <div class="comparison-val">Limitada ao horário da equipe</div>
            <div class="comparison-val good">Total — 24/7 ativo</div>
          </div>
          <div class="comparison-row">
            <div class="comparison-feature">Foco da equipe</div>
            <div class="comparison-val">
              Dividido entre operacional e vendas
            </div>
            <div class="comparison-val good">100% vendas e retenção</div>
          </div>
          <div class="comparison-row">
            <div class="comparison-feature">Custo fixo</div>
            <div class="comparison-val">Alto — pessoal e infraestrutura</div>
            <div class="comparison-val good">Zero para a academia</div>
          </div>
        </div>
      </div>
    </section>

    <!-- Contact -->
    <section id="contato" style="background: var(--cream)">
      <div class="contact-section">
        <div class="contact-left reveal">
          <p class="section-label">Vamos conversar</p>
          <h2 class="section-title" style="margin-bottom: 1.5rem">
            Eleve o nível da sua academia <em>sem elevar os custos.</em>
          </h2>
          <p
            style="
              font-size: 0.95rem;
              color: var(--text-muted);
              line-height: 1.8;
              margin-bottom: 2.5rem;
            "
          >
            Diagnóstico gratuito, instalação rápida e sem burocracia. Nossa
            equipe de expansão entra em contato em até 24 horas.
          </p>
          <div class="contact-benefits">
            <div class="contact-benefit">
              <div class="benefit-check">
                <svg
                  width="12"
                  height="12"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="var(--green)"
                  stroke-width="3"
                >
                  <polyline points="20 6 9 17 4 12"></polyline>
                </svg>
              </div>
              <p class="benefit-text">
                <strong>Diagnóstico gratuito</strong> da sua operação atual sem
                compromisso
              </p>
            </div>
            <div class="contact-benefit">
              <div class="benefit-check">
                <svg
                  width="12"
                  height="12"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="var(--green)"
                  stroke-width="3"
                >
                  <polyline points="20 6 9 17 4 12"></polyline>
                </svg>
              </div>
              <p class="benefit-text">
                <strong>Instalação em 48h</strong> sem interromper a operação da
                academia
              </p>
            </div>
            <div class="contact-benefit">
              <div class="benefit-check">
                <svg
                  width="12"
                  height="12"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="var(--green)"
                  stroke-width="3"
                >
                  <polyline points="20 6 9 17 4 12"></polyline>
                </svg>
              </div>
              <p class="benefit-text">
                <strong>Sem custo fixo</strong> — modelo de parceria com revenue
                share
              </p>
            </div>
            <div class="contact-benefit">
              <div class="benefit-check">
                <svg
                  width="12"
                  height="12"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="var(--green)"
                  stroke-width="3"
                >
                  <polyline points="20 6 9 17 4 12"></polyline>
                </svg>
              </div>
              <p class="benefit-text">
                <strong>Suporte dedicado</strong> e relatórios mensais de
                performance
              </p>
            </div>
          </div>
        </div>
        <div class="reveal reveal-delay-2">
          <div class="contact-form-wrap">
            <p
              style="
                font-family: &quot;Cormorant Garamond&quot;, serif;
                font-size: 1.6rem;
                font-weight: 600;
                color: var(--green-deep);
                margin-bottom: 0.5rem;
              "
            >
              Solicitar proposta
            </p>
            <p
              style="
                font-size: 0.8rem;
                color: var(--text-muted);
                margin-bottom: 2rem;
              "
            >
              Preencha abaixo e retornamos em até 24h.
            </p>

            <form id="contact-form" onsubmit="handleSubmit(event)">
              <div class="form-row">
                <div class="form-group">
                  <label class="form-label">Seu nome</label>
                  <input
                    type="text"
                    class="form-input"
                    placeholder="Roberto Silva"
                    required
                  />
                </div>
                <div class="form-group">
                  <label class="form-label">Nome da academia</label>
                  <input
                    type="text"
                    class="form-input"
                    placeholder="Zest Fitness Club"
                    required
                  />
                </div>
              </div>
              <div class="form-row">
                <div class="form-group">
                  <label class="form-label">E-mail</label>
                  <input
                    type="email"
                    class="form-input"
                    placeholder="parceria@academia.com"
                    required
                  />
                </div>
                <div class="form-group">
                  <label class="form-label">WhatsApp</label>
                  <input
                    type="tel"
                    class="form-input"
                    placeholder="(11) 99999-0000"
                    required
                  />
                </div>
              </div>
              <button type="submit" class="btn-submit">
                Solicitar proposta comercial
              </button>
              <p class="form-disclaimer">
                Diagnóstico gratuito · Sem compromisso · Resposta em 24h
              </p>
            </form>

            <div class="success-msg" id="success-msg">
              <div class="success-icon">
                <svg
                  width="32"
                  height="32"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="white"
                  stroke-width="2.5"
                >
                  <polyline points="20 6 9 17 4 12"></polyline>
                </svg>
              </div>
              <p
                style="
                  font-family: &quot;Cormorant Garamond&quot;, serif;
                  font-size: 2rem;
                  font-weight: 600;
                  color: var(--green-deep);
                  margin-bottom: 0.5rem;
                "
              >
                Mensagem enviada!
              </p>
              <p style="font-size: 0.9rem; color: var(--text-muted)">
                Nossa equipe de expansão entrará em contato em até 24h.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Footer -->
    <footer>
      <div class="footer-inner">
        <div>
          <span
            style="
              font-family: &quot;Cormorant Garamond&quot;, serif;
              font-size: 1.4rem;
              font-weight: 700;
              color: var(--green-deep);
            "
          >
            ZEST
            <em style="font-weight: 400; color: var(--peach)"
              >Wellness Station</em
            >
          </span>
          <p
            style="
              font-size: 0.75rem;
              color: var(--text-muted);
              margin-top: 0.5rem;
              max-width: 280px;
              line-height: 1.6;
            "
          >
            Elevando o padrão de conveniência e nutrição no ecossistema fitness
            brasileiro.
          </p>
        </div>
        <div class="footer-links">
          <a href="#">
            <svg
              width="16"
              height="16"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
            >
              <rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect>
              <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path>
              <line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line>
            </svg>
            Instagram
          </a>
          <a href="#">
            <svg
              width="16"
              height="16"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
            >
              <path
                d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"
              ></path>
              <rect x="2" y="9" width="4" height="12"></rect>
              <circle cx="4" cy="4" r="2"></circle>
            </svg>
            LinkedIn
          </a>
        </div>
        <p class="footer-copy">
          © 2024 Zest Wellness Station. Todos os direitos reservados.
        </p>
      </div>
    </footer>

    <!-- Mobile CTA -->
    <div
      class="mobile-cta"
      id="mobile-cta"
      style="opacity: 0; transition: opacity 0.4s ease"
    >
      <a href="#contato">Quero ser parceiro →</a>
    </div>

    <script>
      // Nav scroll
      const nav = document.getElementById("nav");
      window.addEventListener("scroll", () => {
        nav.classList.toggle("scrolled", window.scrollY > 60);

        // Mobile CTA reveal after scrolling past hero
        const cta = document.getElementById("mobile-cta");
        cta.style.opacity =
          window.scrollY > window.innerHeight * 0.8 ? "1" : "0";
      });

      // Intersection Observer for reveal
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((e) => {
            if (e.isIntersecting) {
              e.target.classList.add("visible");
              observer.unobserve(e.target);
            }
          });
        },
        { threshold: 0.12 },
      );

      document
        .querySelectorAll(".reveal")
        .forEach((el) => observer.observe(el));

      // Counter animation
      function animateCounter(el) {
        const target = parseInt(el.dataset.target ?? el.textContent);
        const prefix = el.dataset.prefix ?? "";
        const suffix = el.dataset.suffix ?? "";
        const duration = 1800;
        const start = performance.now();

        const tick = (now) => {
          const progress = Math.min((now - start) / duration, 1);
          const eased = 1 - Math.pow(1 - progress, 3);
          el.textContent = prefix + Math.round(eased * target) + suffix;
          if (progress < 1) requestAnimationFrame(tick);
        };
        requestAnimationFrame(tick);
      }

      const counterObserver = new IntersectionObserver(
        (entries) => {
          entries.forEach((e) => {
            if (e.isIntersecting) {
              animateCounter(e.target);
              counterObserver.unobserve(e.target);
            }
          });
        },
        { threshold: 0.5 },
      );

      document.querySelectorAll(".counter, [data-count]").forEach((el) => {
        // Store original target from data-count for hero stats
        if (el.dataset.count !== undefined) {
          el.dataset.target = el.dataset.count;
        }
        counterObserver.observe(el);
      });

      // Hero stat counters
      document.querySelectorAll("[data-count]").forEach((el) => {
        const target = parseInt(el.dataset.count);
        const suffix = el.dataset.suffix ?? "";
        el.dataset.target = target;
        if (!el.classList.contains("counter")) {
          el.classList.add("counter");
          // override textContent default
          el.textContent = "0" + suffix;
        }
      });

      // Form submit
      function handleSubmit(e) {
        e.preventDefault();
        document.getElementById("contact-form").style.display = "none";
        document.getElementById("success-msg").style.display = "block";
      }
    </script>
  </body>
</html>
